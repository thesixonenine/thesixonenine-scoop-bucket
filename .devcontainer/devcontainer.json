{
  "name": "scoop-bucket",
  "runArgs": ["--name", "scoop-bucket"],
  "build": {
    "dockerfile": "Dockerfile",
    "args": { "GITHUB_PAT": "${localEnv:GITHUB_PAT}" }
  },
  "postStartCommand": "chezmoi update",
  "customizations": { "vscode": { "settings": {
        "extensions.allowed": {
            "microsoft": false,
            "github": false
        }
  }}}
}
